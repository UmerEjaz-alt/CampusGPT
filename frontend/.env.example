# Frontend environment variables
# Copy to .env for local overrides (optional in dev — Vite proxies /api to backend)

# Production: set to your deployed backend URL
# VITE_API_URL=https://your-backend.example.com

# Development: leave unset to use Vite proxy (see vite.config.ts)
VITE_API_URL=
